This is index file
<% @posts.each do |Post| %>
	<div class = "post_wrapper">
	<h2 class = "title"><%= link_to post.title,post %></h2>
	<p class = "date"><%= post.created_at.startime("%B,%d,%y") %></p>
	</div>
	<% end %>